<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>

    <!--Import Google Icon Font-->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet"> 
    <!--Import materialize.css-->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link rel='stylesheet' href='css/main.css' />
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  </head>
  <body>

    <div class="container pt-3">
      <div class="row">
        <div class="col">
          <h1><%= title %></h1>
        </div>
      </div>
      <% if (messages.error) { %>
        <div class="alert alert-danger" role="alert">
          <%= messages.error %> 
        </div>
      <% } %>
      <% if (!user) { %>
      <div class="row">
        <div class="col">
          <h2>Login</h2>
        </div>
      </div>
      <div class="row">
          <form class="col" method="POST" action="/login">
            <div class="row">
              <div class="form-group col">
                <label for="username">Name</label>
                <input name="username" id="username" type="text" class="form-control">

              </div>
              <div class="form-group col">
                <label for="password">Password</label>
                <input name="password" id="password" type="text" class="form-control">
              </div>
            </div>
            <div class="row">
              <div class="col s12">
                <button class="btn btn-primary btn-block" type="submit" name="action">Login</button>
              </div>
            </div>
          </form>
      </div>
      <% } else { %>
        <div class="row" id="welcome">
          <div class="col mr-md-auto">
            <span class="align-bottom h4">Welcome, <%= user.name %></span>
          </div>
          <div class="col-auto">
            <% if (user.permission != 2 || (user.permission == 2 && userList.filter((val) => val.permission == 2).length > 1)) { %>
              <a class="btn btn-danger" href="#">Delete Account</a>
            <% } %>
            <a class="btn btn-primary" href="/logout">Logout</a>
          </div>
        </div>
        <hr/>
        <div class="row">
          <div class="col-6">
            <div><b>Client ID</b></div>
            <small class="text-muted">This is the ID you use to access the API</small>
            <% if (user.clientId != "") { %>
              <p class="text-break"><%= user.clientId %></p>
            <% } else { %>
              <p>-Not Generated Yet-</p>
            <% } %> 
          </div>
          <div class="col-6">
            <div><b>Client Secret</b></div>
            <small class="text-muted">This is what you'll send in the Authorization header when connecting to the API</small>
            <% if (user.clientSecret != "") { %>
              <p class="text-break"><%= user.clientSecret %></p>
            <% } else { %>
              <p>-Not Generated Yet-</p>
            <% } %> 
          </div>
        </div>
        <div class="row">
          <div class="col">
            <a class="btn btn-primary btn-block" type="button" href="/generate">Revoke and Regenerate</a>
          </div>
        </div>
        <% if (user.permission == 2) { %>
          <hr/>
          <!--Navigation-->
          <div class="row">
            <div class="col">
              <ul class="nav nav-justified">
                <li class="nav-item">
                  <a class="nav-link active" href="#researcherList">Active</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="#">Link</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="#">Link</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
                </li>
              </ul>
            </div>
          </div>
          <!--End Navigation-->
          <hr/>
          <!--Researcher List-->
          <div class="row" id="researcherList">
            <div class="col">
              <h2>Researchers</h2>
              <p><%= userList.length %> entries</p>
            </div>
          </div>
          <div class="row">
            <div class="col">
              <table class="table table-hover">
                <thead class="thead-dark">
                  <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    <th scope="col">Client ID</th>
                    <th scope="col">Client Secret</th>
                    <th scope="col">Actions</th>
                  </tr>
                </thead>
                <tbody>
                  <% for( let idx = 0; idx < userList.length; idx++ ) { %>
                    <tr>
                      <th scope="row"><%= idx + 1 %></th>
                      <td><%= userList[idx].name %></td>
                      <% if (userList[idx].clientId != "") { %>
                        <td class="text-break"><%= userList[idx].clientId %></td>
                      <% } else { %>
                        <td>-</td>
                      <% } %> 
                      <% if (userList[idx].clientSecret != "") { %>
                        <td class="text-break"><%= userList[idx].clientSecret %></td>
                      <% } else { %>
                        <td>-</td>
                      <% } %>
                      <td><a class="btn btn-primary" href="#">Test</a></td>
                    </tr>
                  <% } %>
                </tbody>
              </table>
            </div>
          </div>
          <div class="row" id="researcherList">
            <div class="col">
              <a href="#welcome">Back to Top</a>
            </div>
          </div>
          <!--End Researcher List-->
        <% } %>
      <% } %>
    </div>

    <!--JavaScript at end of body for optimized loading-->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
  </body>
</html>
